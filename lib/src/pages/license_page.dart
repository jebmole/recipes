import 'package:flutter/material.dart';

class MyLicensePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Created by Jonathan"),
    );
  }
}
